<h2>Recent Strips</h2>

: if $strip_list.size() > 0 {
:   include $op.tmpl( $app_path, 'paginate_strips' ) { strip_list => $app.completed_strips, pag_path => $app_path ~ '/p/recent' }
: } else {
    <h4>No Completed Strips Yet</h4>
: }
