to make seamless between clien and server...
  pass in arguments to the called function
   normal scalars, lists, hashes and yote objects
   but also functions for file objects

   the function could look like
       yote.include_files( '.filecontrols' ) which returns functions that return the
       file objects

on the other side, those functions come across as files

____________________________________________________________

mod perl?
  apt install libapache2-mod-perl2

PerlPostConfigRequire /home/wolf/open_source/EPUC/setup.pl

    <Location /epucc>
        SetHandler perl-script
        PerlHandler EPUC::Test
    </Location>

find if a request method is attached or not? try

 perl -MModPerl::MethodLookup -e print_method content_type
 perl -MModPerl::MethodLookup -e print_method print

sudo apt install libapache2-mod-perl2-dev
sudo apt install libapache2-mod-apreq2
sudo apt install libapache2-request-perl
apt-get install apache2-dev
get Apache2::Cookie from cpan

Dave Rhodes landline 323 469 5922
